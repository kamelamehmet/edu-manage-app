# PocketBase backend environment example
# Copy this to backend/.env and adjust values as needed.

# The address PocketBase should bind to (default: 127.0.0.1)
PB_ADDRESS=127.0.0.1

# The port PocketBase should serve on (default: 8090)
PB_PORT=8090

# Public URL used in email links / external access
PB_PUBLIC_URL=http://localhost:8090

# Optional: admin credentials for initial superuser creation
# PB_ADMIN_EMAIL=admin@example.com
# PB_ADMIN_PASS=ChangeMe123!
